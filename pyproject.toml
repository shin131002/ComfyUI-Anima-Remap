[project]
name = "anima-remap"
description = "Use LoRAs, model merges and ControlNets (LLLite/VACE) across Anima generations (Anima, 2.9B, 3.8B/Pro52 and beyond) with automatic block-index remapping"
version = "1.4.0"
license = {file = "LICENSE"}

classifiers = [
    "Operating System :: OS Independent",
    "Environment :: GPU :: NVIDIA CUDA",
    "Environment :: GPU :: AMD ROCm",
]

[project.urls]
Repository = "https://github.com/shin131002/ComfyUI-Anima-Remap"

[tool.comfy]
PublisherId = "konohana"
DisplayName = "ComfyUI Anima Remap"
Icon = ""
