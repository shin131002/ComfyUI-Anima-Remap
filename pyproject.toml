[project]
name = "anima-remap"
description = "Auto block-index remapping for applying LoRAs and merging models across the Anima model family (Anima, Anima-2.9B, Anima-3.8B/Pro52, and future generations)"
version = "1.2.0"
license = {file = "LICENSE"}

classifiers = [
    "Operating System :: OS Independent",
    "Environment :: GPU :: NVIDIA CUDA",
    "Environment :: GPU :: AMD ROCm",
]

[project.urls]
Repository = "https://github.com/shin131002/ComfyUI-Anima-Remap"

[tool.comfy]
PublisherId = "konohana"
DisplayName = "ComfyUI Anima Remap"
Icon = ""
